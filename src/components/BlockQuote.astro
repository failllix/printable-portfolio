---
import { twMerge } from "tailwind-merge";

const { className } = Astro.props;
---

<blockquote
  class={twMerge("my-3 opacity-70 border-l-2 border-gray-700 pl-3", className)}
>
  <slot />
</blockquote>
