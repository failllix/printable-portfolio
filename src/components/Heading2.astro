---
import { twMerge } from "tailwind-merge";

const { className, id, compact } = Astro.props;
---

<h2
  id={id}
  class={twMerge(
    `${compact ? "text-xl mb-1 mt-1" : "text-2xl mb-2 mt-3"} font-bold`,
    className
  )}
>
  <slot />
</h2>
